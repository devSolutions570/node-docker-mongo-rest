export type Nutrition = {
    _id: string;
    dessertName: string;
    calories: string;
    fat: string;
    carbs: string;
    protein: string;
};

export type Success = {
    success: string,
    error: string,
}